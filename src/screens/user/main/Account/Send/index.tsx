import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

const Send = () => {
  return (
    <View>
      <Text>Send</Text>
    </View>
  );
};

export default Send;

const styles = StyleSheet.create({});
